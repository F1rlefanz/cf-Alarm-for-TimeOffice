<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Existing permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    
    <!-- Alarm & Wake permissions - Optimal Strategy -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Multi-API Alarm Permissions Strategy -->
    <!-- For API 31-32 (Android 12-13) -->
    <uses-permission 
        android:name="android.permission.SCHEDULE_EXACT_ALARM" 
        android:maxSdkVersion="32" />
    
    <!-- For API 33+ (Android 13+) - The Optimal Way -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:name=".CFAlarmApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CFAlarmForTimeOffice"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CFAlarmForTimeOffice">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- AlarmFullScreenActivity - Shows when alarm goes off -->
        <activity
            android:name=".AlarmFullScreenActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:theme="@style/Theme.CFAlarmForTimeOffice"
            android:showWhenLocked="true"
            android:turnScreenOn="true" />
        
        <!-- AlarmReceiver - Handles alarm events -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false" />
            
        <!-- AlarmStopService - Stops alarm when user dismisses -->
        <service
            android:name=".AlarmStopService"
            android:exported="false" />
        
    </application>

</manifest>